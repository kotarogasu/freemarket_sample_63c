- if @card.blank?
  .buy-content__second
    .buy-content__second__text
      %h3 支払い方法
      %p.buy-user-info-tex
        = link_to addition_card_index_path,class:'addition_card_link' do
          %i.fas.fa-plus-circle
          %span 登録してください
- else
  .buy-content__second
    .buy-content__second__text
      %h3 支払い方法              
      %p.buy-user-info-text
        クレジットカード
        %br/
        = "**** **** **** " + @default_card_information.last4
        %br
        有効期限
        - exp_month = @default_card_information.exp_month.to_s
        - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
        = exp_month + " / " + exp_year
        %br/
        = image_tag "credit/Visa.png", width:"30px", height:"30px"
        = link_to card_path, class:"buy-user-info-fix" do
          %span 変更する
          %i.fa.fa-chevron-right
